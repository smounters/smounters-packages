# Changelog

## Не выпущено

### Added
- Первые фрагменты: `BaseEntity` (первичный ключ UUIDv7), `AuditableEntity` (+ колонки аудита),
  `UserSettingBase` (мешок настроек пользователя: секция/ключ/JSON-значение).

Пакет пока **не публикуется** — его нет в матрице `publish.yml`. Проверен на 111 сущностях
`smounters-apps`: после замены локального объявления базового класса на фрагмент из собранного пакета
схема не изменилась (расхождение метаданных с живой базой совпало до и после), типизация прошла.
