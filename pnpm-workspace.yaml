packages:
  - "packages/*"

# esbuild нужен vitest/vitepress; @swc/core — трансформер тестов, эмитит
# emitDecoratorMetadata (esbuild этого не умеет, без него DI в тестах падает).
allowBuilds:
  esbuild: true
  "@swc/core": true
